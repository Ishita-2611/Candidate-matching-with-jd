{
  "name": "candidate-ranking-submission",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "precompute:retrieval": "node precompute-retrieval.js",
    "rank": "python rank.py --candidates candidates.jsonl --retrieval retrieval_candidates.json --out submission.csv",
    "test": "node --check precompute-retrieval.js && python -m py_compile rank.py"
  },
  "license": "UNLICENSED",
  "description": "Offline Redrob hackathon candidate ranking submission"
}
